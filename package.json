{
  "name": "sonic-runner",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "kaplay": "^3001.0.0-alpha.21"
  }
}